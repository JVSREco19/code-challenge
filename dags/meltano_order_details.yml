version: 1
plugins:
  extractors:
    - name: tap-csv
      pip_url: tap-csv
      config:
        file_path: "/opt/airflow/data/csv/{{ ds }}/order_details.csv"
  loaders:
    - name: target-postgres
      pip_url: target-postgres
      config:
        host: db
        database: northwind
        user: northwind_user
        password: thewindisblowing
        table: order_details_final
